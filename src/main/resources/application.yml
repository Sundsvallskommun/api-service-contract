spring:
  application:
    name: api-contract
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate

openapi:
  name: ${spring.application.name}
  title: ${spring.application.name}
  version: '@project.version@'
