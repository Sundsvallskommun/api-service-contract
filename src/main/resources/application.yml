spring:
  application:
    name: api-contract
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate

dept44:
  models:
    api:
      paging:
        max:
          limit: 100

openapi:
  name: ${spring.application.name}
  title: Contracts
  version: '@project.version@'
